package com.sainath.java.examples.inheritance;

public interface AnimalFeatures {

	String eatHabits(Animal animal);
	String getSounds(Animal animal);
	int getAge(Animal animal);
	void setBirthDate(Animal animal, String dob);
	
}
